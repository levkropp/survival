/*
 * app_files.h — File browser app
 */
#ifndef APP_FILES_H
#define APP_FILES_H

/* Run the file browser. Initializes SD card, detects filesystem,
 * and lets the user navigate directories with touch. */
void app_files_run(void);

#endif /* APP_FILES_H */
